Trainer(ModelCheckpointer, Logger, EarlyStopper)
    fit(Task, DataLoader, DataLoader) -> Model path
    test(Task, DataLoader) -> Result path

Task(Network, Criterion, Optimizer, LRScheduler, Regulerizer, Metrics)  # TODO: SWA, Augmix
    Classification
    Segmentation

Result()

MetricsCombiner()


vision/
    DataLoader(Dataset, Sampler)

    Dataset(Annotation, Transform)
        ClassificationDataset()
        SegmentationDataset()

    Annotation()
        SingleImageAnnotation()
        MultiImageAnnotation()
        PairImageAnnotation()

    Transform()

    Sampler()
        RandomSampler()
        WeightedSampler()


visualizer/
    GradCAM(Network, layer_name) -> checkpoint_path (of mask images)

    ActivationMap(Network, layer_names) -> checkpoint_path (of mask images)
